specVersion: "v1beta"
serviceId: "cricron"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "cricron-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
